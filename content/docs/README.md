# Shyena Documentation Content

Documentation is now intended to be maintained as version-controlled content artifacts and published through the SAGE pipeline. Each published document must contain frontmatter with `title`, `description`, and `slug` and must pass the content contract before merging.

Current application routes remain compatible with the existing TanStack Router pages while the content-engine migration is introduced incrementally.
