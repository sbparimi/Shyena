name: SAGE Content Validation

on:
  pull_request:
    paths:
      - "content/**"
      - "src/lib/sage/**"
      - "scripts/validate-sage-content.mjs"
      - "package.json"
      - "bun.lock"

jobs:
  content-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm install
      - run: node scripts/validate-sage-content.mjs
      - run: npm run build
